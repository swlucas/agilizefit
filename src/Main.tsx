import React from "react";
import { View, Text } from "react-native";
import Camera from "./components/Camera";

const Main = () => {
  return (
    <View>
      <Camera />
    </View>
  );
};

export default Main;
